spring:
  application:
    name: call
  datasource:
    url: jdbc:mysql://localhost:3306/telecom?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: angel1052
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  logging:
    file:
      name: "logs/telecom.log"
    level:
      org.springframework: WARN
      com.teleco: INFO
