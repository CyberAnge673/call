spring:
  application:
    name: call
  datasource:
    url: jdbc:mysql://localhost:3307/telecom?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: angel1052
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
asterisk:
  ami:
    host: localhost
    port: 5038
    username: admin
    password: password

logging:
  file:
    name: "logs/telecom.log"
  level:
    root: WARN
    org.springframework: WARN
    org.springframework.web: ERROR
    org.hibernate: ERROR
    com.telecom: INFO
